// The actual game engine, all calls to the engine should go through this.
var PENULT_GAME_ENGINE;

// Tweakable constants
var BG_CLEAR_COLOR = "Blue";
var ANIM_TIMEOUT = 100;

// Probably shouldn't change these